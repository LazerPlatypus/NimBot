module.exports = {
    name: 'takesticks',
    description: 'Take a stick from a row in the game;',
    guildOnly: false,
    args: true,
    cooldown: false,
    execute(message, args) {
        const Discord = require('discord.js');
        var row = args[0];
        var amount = args[1];
        




    }
};

